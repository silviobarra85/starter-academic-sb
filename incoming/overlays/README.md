# Overlay in ingresso

Carica qui gli zip overlay generati dall'assistente.

Quando uno zip viene caricato e committato su GitHub, il workflow `.github/workflows/apply-overlay.yml` lo applica automaticamente, esegue gli audit disponibili, committa le modifiche e rimuove lo zip processato.
