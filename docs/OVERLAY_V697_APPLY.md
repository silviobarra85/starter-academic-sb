# Overlay V697 - ioSudo globale v44

Applicazione manuale:

```bash
cp -R ./static/* static/
cp -R ./docs/* docs/
```

Commit suggerito:

```bash
git add static docs
git commit -m "Aggiorna ioSudo V697"
git push origin master
```
