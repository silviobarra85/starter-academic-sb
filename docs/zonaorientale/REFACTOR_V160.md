# V160 - Mobile rose e titolo competizione

Modifiche solo mobile:

- in Rose, quando si apre una rosa, la colonna `Giocatore` della tabella giocatori e stata ridotta sensibilmente;
- nella tabella Movimenti, il badge `Budget Iniziale` viene mostrato su due righe (`Budget` / `Iniziale`);
- quando il movimento e `INITIAL_BUDGET`, la colonna `Giocatore` mostra `-`;
- nella pagina singola competizione e stato aggiunto un header mobile con il nome della competizione e i pulsanti:
  - `Torna a tutte le competizioni`;
  - `Torna alla Home`.

Desktop invariato.
